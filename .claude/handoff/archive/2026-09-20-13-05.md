# Handover — 2026-09-20 13:05 | Claude Opus 5

## Mode
Task complete — Story 13.3 merged; Epic 13's re-cut is in main; nothing mid-flight.

## Focus
- **Task**: Story 13.3 close-out, then the workflow fixes it triggered
- **Branch**: `main` (all three PRs merged); this handover lands on
  `claude/post-merge-13-3-close-2026-09-20`
- **State**: complete
- **Progress**: Epic 13 at 3/17 (re-cut from 8 stories to 17 this session)

## Resume Point
Start Story 13.4a, `every-member-row-resolves-to-exactly-one-brand` — the first story
under the new rules. Run `bmad-create-story` FIRST: `test_an_active_story_has_a_story_file`
fails if 13.4a leaves `backlog` without `_bmad-output/implementation-artifacts/
13-4a-every-member-row-resolves-to-exactly-one-brand.md`. It is identity only (no new
figures), so it can merge before any computation exists.

## Uncommitted Files
None — working tree clean.

## What Happened This Session
- Story 13.3 merged (#138) after two Codex rounds. The second found the brands-only fix
  half-done: CPB's residual bucket still landed in the brands concept, and unmapped_member
  suppression keyed on member NAME. Both fixed in `concepts_v17`.
- Diagnosed throughput against `reslint`: 13.3 took 19 commits/8 days; reslint averages
  1.15 commits/story with a story file for all 92. `bmad-create-story` had lapsed after
  Epic 5, so stories had no exit condition. Fixed in #141 and enforced by a test.
- Approval audit: 47% of 3,254 recorded Bash calls are unmatchable by any permission rule
  (39% chain 3+ segments, 13% heredocs, 8% source `.env`). #141 adds a `Makefile` so one
  rule, `Bash(make *)`, covers the lot.
- Epic 13 re-cut (#142): 13.4–13.8 became 14 one-outcome stories. Found two ownership
  gaps — segment concept mapping was owned by NO story (now 13.5a), and 13.5 referenced
  a store 13.3 already built.
- A near-miss: the first DB guard compared DSN TEXT, so `...thesistrace?sslmode=disable`
  passed and started a run against the dev store. Guard moved into `conftest.py` beside
  the `drop_all` and now compares (host, port, database). CI got its own migration
  database rather than an exemption.

## Decisions Made
- Brands concept holds named brands only: residual → `..._residual`, aggregate → `..._total`.
- Story files mandatory (test-enforced from 13.4); one outcome per story; ship the fix and
  defer the finding; ask blocking decisions immediately with a recommendation.
- Lettered story keys (13.4a) keep split lineage; three tracker regexes accept them.
- Lawrence, 2026-09-20: sourcing `.env` and running against the dev DB needs no per-command
  approval. Values still stay out of transcripts and commits. Secrets get rotated before
  the repo goes public.

## Context Needed
- **Docker Desktop is DOWN** — the DB suite skips locally (165 skips, not 143). CI covers it.
- Use `make` now, not command chains: `make test`, `make api` (:8001), `make web` (:3001),
  `make py F=...`. Never heredocs — that is CLAUDE.md's "Running things" rule.
- PRs #139 and #140 belong to another session (worktree `../ThesisTrace-envfile`).
- `2e6de15` (the residual fix) was merged WITHOUT a Codex round, on Lawrence's call.
- Deferred finding with a home: `story_13_3_member_exclusions_are_not_axis_scoped` → 13.4b.

## Next Action
`git checkout -b claude/13-4a-brand-identity-2026-09-20`, run `bmad-create-story` for
13.4a, then flip it to `in-progress` in `sprint-status.yaml:218`.

## References
- `_bmad-output/planning-artifacts/epics.md` (`### Story 13.4a`)
- `_bmad-output/implementation-artifacts/sprint-status.yaml:217`
- `_bmad-output/implementation-artifacts/engineering-findings.yaml`
- `CLAUDE.md` — "Story workflow" and "Running things"
- `.claude/context/project-context.md` (2026-09-18 and 2026-09-20 learnings)

---
*Generated by /session-end — 2026-09-20 13:05*
