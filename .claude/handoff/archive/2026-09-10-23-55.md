# Handover — 2026-09-10 23:55 | Claude Opus 5

## Mode
Task complete — clean stopping point. Nothing half-built, nothing uncommitted.

## Focus
- **Task**: Epic 13 (acquisition performance) — decomposed, then Stories 13.1 and 13.2 built
- **Branch**: `main` (a sprint-status commit is pending push — see Uncommitted)
- **State**: complete — 4 PRs merged this session, no open PRs
- **Progress**: Epic 13 is **2/8**; project is 63/69 stories, 9/13 epics

## Resume Point
**Start Story 13.3 — brand-intangible concept mapping across the US-GAAP filers.**
Read its acceptance criteria in `epics.md` (`### Story 13.3`). It owns axis/member-aware
mapping END TO END: the YAML spec representation and `MappingRule`, the
`SOURCE_TO_CANONICAL` lookup canonicalization actually reads, the `concept_mappings`
audit projection and its migration, selection behaviour, and their tests.

**Do NOT assume `concept_mappings` drives selection — it does not.** `canonicalize.py:187`
reads `SOURCE_TO_CANONICAL`, built from the YAML specs via `mappings/engine.py:291`; the
table is an audit projection. A migration alone changes nothing. (I got this wrong in the
13.1 plan; Codex caught it.)

**13.3 carries a BLOCKING question the epic's headline depends on:** is impairment
brand-dimensioned at all? CPB tags
`ImpairmentOfIntangibleAssetsIndefinitelivedExcludingGoodwill` under Corporate & Other,
not under a trademark member. If impairment is only ever filer- or segment-level, then
"was this brand written down?" is not answerable per brand, and 13.4/13.7 must say so
plainly. Resolve from live filings, not by assumption.

Dimensional data is now ingested and reachable: `ingestion/inline_xbrl.py`, wired into
`pipeline/run.py`, fixtures in `backend/tests/fixtures/*_instance.xml`.

## Uncommitted Files
`_bmad-output/implementation-artifacts/sprint-status.yaml` — marks 13.2 `done` after the
#136 merge. Committed? NO — commit and push it first thing.

## What Happened This Session
- **Decomposed Epic 13** into 8 stories (#133), after verifying four architectural seams
- **Closed D3.2** (#134) — the CPB packet's section 6 records "yes, it changed the
  decision": an intent to buy, reversed. **Phase 1 met its own success definition for the
  first time since July.**
- **Story 13.1** (#135) — AD-3 rule 0, AD-4 extension, contamination guard. Both
  contamination modes reproduced against the real canonicalizer before fixing.
- **Story 13.2** (#136) — Inline XBRL ingestion. Live fetch reproduced every 2026-09-08
  gate finding exactly (CPB 601 / QSR 562 / ZTS 473 / SHOP 333 / CP 640 contexts).
- **Context audit** — found the `CURATED_SECTIONS` guard was one-directional, leaving 20
  of 47 findings unprotected; added `test_every_finding_is_curated` + a duplicate-key
  loader, and a `superseded_by:` convention.

## Decisions Made
- **NEVER add `Co-Authored-By: Claude` or `Claude-Session:` trailers to commits**, any
  repo. Lawrence, 2026-09-09. PR descriptions were not covered.
- **Every Codex prompt ends with a fix-commit-report section** — Codex fixes what it finds,
  commits, reports. Still never merges; that stays Lawrence's call.
- Epics 7-9 stay `blocked`. D9 selects ONE epic per packet; CPB's selected Epic 13. Their
  gate is now a SECOND completed packet.

## Context Needed
- **My recurring failure mode this session: shipping code that is not CONNECTED.** Story
  13.2 was entirely unreachable from `pipeline/run.py`; 13.1 left `dimensions` unmapped in
  `persist_company_facts`. Both caught by Codex, not by me or the suite. **Treat "what
  calls this?" as an acceptance criterion before writing tests.**
- Live EDGAR fetches need `EDGAR_CONTACT` exported (it is in `.env` at repo ROOT, and
  `get_settings()` resolves `.env` relative to CWD, so running from `backend/` misses it).
  Same for `TEST_DATABASE_URL` — and never run pytest with only `DATABASE_URL` set.
- Two open findings: `dimensioned_facts_would_contaminate_consolidated_canonical_facts`
  (closed in practice by 13.1; status not yet flipped) and
  `ad3_decimals_tiebreak_has_never_had_data` (real, deliberately deferred).
- `project-context.md` is at 197/200 lines with no headroom; two declined trims are
  recorded in `.claude/archive/optimization-records/2026-09-09.yaml`.
- Deletable leftover: local ref `backup/pre-trailer-strip-2026-09-09`.

## Next Action
`git add _bmad-output/implementation-artifacts/sprint-status.yaml && git commit && git push`
— then branch for Story 13.3 off a fresh `main`.

## References
- `_bmad-output/planning-artifacts/epics.md` (`### Story 13.3`)
- `backend/canonicalization/mappings/specs/us-gaap_v12.yaml`, `mappings/engine.py`
- `backend/ingestion/inline_xbrl.py`, `backend/tests/test_inline_xbrl.py`
- `_bmad-output/implementation-artifacts/engineering-findings.yaml` (48 entries)
- `.claude/context/project-context.md`

---
*Generated by /session-end — 2026-09-10*
