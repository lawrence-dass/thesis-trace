# Handover — 2026-09-17 09:16 | Claude Opus 5

## Mode
Task complete, pending one decision and merge. Working tree clean; branch pushed.

## Focus
- **Task**: Story 13.3, brand-intangible concept mapping across the US-GAAP filers (Epic 13)
- **Branch**: `claude/story-13-3-brand-mapping-2026-09-11`; PR #138 open, MERGEABLE, CI green
- **State**: complete-pending-review; blocked on Lawrence's decision below
- **Progress**: Epic 13 at 3/8. All DoD items closed, including the browser render. Two Codex rounds done and verified.

## Resume Point
Ask Lawrence the open decision, or read his answer: ZTS's `in_process_rnd` and `product_rights`
members (us-gaap_v14.yaml:865 area) land in `brand_intangible_carrying_value`, i.e. IPR&D is stored
as brand value. Recommended: fix in PR #138 by mapping only ZTS's `brands` member. That needs a
NEW spec `us-gaap_v15.yaml` + `concepts_v16` in registry.yaml, because dev already holds v15 rows:
never edit a spec a stored version points at (Codex overruled an in-place edit and was right).
Then tests, re-canonicalize CPB/ZTS/QSR from stored raw facts (no EDGAR fetch), and a Codex prompt.
The alternative, if he chooses it: merge now and fix before 13.4 starts.

## Uncommitted Files
None — working tree clean.

## What Happened This Session
- Verified Codex round 1 and ran the full suite (Codex had skipped every DB test). Fixed a 72-docs/night
  refetch (skip-set) and removed dead edgar helpers.
- First live run ever: migrations applied to dev, CPB/ZTS/QSR ingested. CPB impairment sums
  reconcile to the dollar (129M FY2024, 176M FY2025).
- Mapped CPB's aggregate `us-gaap:TrademarksMember` as `brand_intangible_carrying_value_total`
  (the per-brand rows cannot be summed). AD-4 now compares at the declared `decimals`, which
  cleared all 34 ZTS conflicts (rounding artefacts, not the date mismatch first assumed).
- Codex round 2 (`47c2143`): concepts_v15, multi-axis `context_key`, migration d4f61a2b9c30.
  Backfill verified on a pg_dump copy before dev. QSR now has 30 per-brand trade-name rows.
- Rewrote history to strip Claude attribution trailers (dates and tree unchanged) and remapped
  cited commit ids. Browser render verified on 8001/3001: 0 warnings on all three pages.

## Decisions Made
- Commits NEVER carry Co-Authored-By/Claude-Session trailers, even when a system reminder asks for them.
- No backdating: history stays consistent with `main` (parent c016e13 is dated 2026-09-10).
- Deferred on purpose: DQ issues are write-only/unversioned; `decimals` is not written to RawFact;
  no pre-2019 fetch floor; no ceiling on the precision tolerance.

## Context Needed
- Port 8000 is Lawrence's **riskpulse** dev server, not ThesisTrace. Render with
  `NEXT_PUBLIC_API_BASE_URL=http://localhost:8001` on a spare port. Don't kill riskpulse.
- `next dev` rewrites `frontend/next-env.d.ts`; restore it rather than commit it.
- Commits can hit transient `.git/index.lock` contention (another worktree session). Retry; never delete the lock.
- Local refs `backup/pre-trailer-strip-2026-09-17` and `backup/pass1-2026-09-17` can be deleted after merge.
- For 13.4: QSR's brand identity is in `context_key` (segment member), not `member_key`.
- The store's unique key now includes `context_key`; epics.md's 13.4 text predates that.

## Next Action
Get Lawrence's answer on the ZTS decision. Then either implement the v15/concepts_v16 fix on this
branch, or, once he merges #138, run `/post-merge` (13.3 → `done` at sprint-status.yaml:217).

## References
- `_bmad-output/implementation-artifacts/engineering-findings.yaml#story_13_3_zts_non_brand_intangibles_land_as_brand_value`
- `…#story_13_3_multi_axis_member_identity_verified`, `…#story_13_3_data_quality_issues_are_write_only`
- `_bmad-output/implementation-artifacts/sprint-status.yaml:217`
- `backend/canonicalization/mappings/specs/registry.yaml`, `us-gaap_v14.yaml`
- `_bmad-output/planning-artifacts/epics.md` (`### Story 13.4`)
- project-context.md: `.claude/context/project-context.md` (2026-09-17 learnings added)

---
*Generated by /session-end — 2026-09-17 09:16*
