# Handover — 2026-08-13 19:02 | Claude Opus 5

## Mode
Task complete — five PRs open, all green, none merged. Nothing in flight.

## Focus
- **Task**: post-Epic-6 cleanup. The agent work list is now EMPTY.
- **Branch**: `claude/epic-6-retro-and-handover-fix-2026-08-13` (this handover);
  four other session branches carry the rest.
- **State**: complete, pending review + merge.

## ⚠️ Epic 7 is NOT unblocked
Earlier handovers said "Nothing blocks Epic 7". FALSE. `epic_catalog` has it
`deferred_under: D9`, decomposable only once a decision packet (D10) names the
research failure that selects it. Epics 8 and 9 are gated identically. Check
`epic_catalog`, never `development_status` — the latter reads `backlog` because
`EPIC_STATUSES` has no `blocked` value.

## The one thing blocking the roadmap
`_bmad-output/decision-packets/` holds only `TEMPLATE.md`. A packet IS D3.2
("informs one real investment decision by user zero"), open since Phase 1 — so
Phase 1 has never met its own success definition. Needs a real judgment on real
money, written before opening the app. An agent cannot produce it.

## Open PRs — suggested merge order
- **#69** reverse DCF persisted (AD-1) — merge first, **#73 stacks on it**
- **#73** Track P: 2,092 queries → 54; retargets to `main` when #69 lands
- **#70** this retrospective + Epic 7 correction   **#71** frontend test runner
- **#72** weekend-date guard + backfill (already APPLIED to the dev store)

## Uncommitted Files
None — working tree clean.

## What Happened This Session
- Reverse DCF moved off the read path onto the pipeline; 2 tables + migration.
- Epic 6 retrospective: 10 learnings in `project-context.md`.
- Frontend went from no test runner at all to Vitest + 31 tests + a CI step.
- 24 weekend-dated market/FX rows re-dated on the dev store; guard added.
- Overview N+1 removed: cost no longer scales with history at all.

## Decisions Made
- **Relabel in place, never delete-and-recreate** for observation dates.
  `score_inputs.market_price_id/.fx_rate_id` are AD-19 provenance links (69 + 38
  live); deleting orphans them. It is the same observation, only mislabelled.
- **Rate columns are `NUMERIC(18,10)`**, not the money-shaped `(28,6)` — the
  solver's tolerance is 1e-7 and `(28,6)` rounds coarser than the bisection.
- **Provenance is now explicitly ordered** (`fiscal_year DESC, concept`). The old
  order was physical row order and unreproducible — `score_inputs` has a random
  UUID PK and no sequence column.

## Context Needed
- **DB tests need `TEST_DATABASE_URL` EXPORTED** — conftest reads `os.environ`
  only, so they skip silently (219 instead of ~329).
- `test_health_db_503_when_unconfigured` fails locally, NOT a regression — the
  gitignored root `.env` sits below the env layer; CI has none.
- Dev store: migrated, reverse DCF materialized, observation dates backfilled.
  Its `reverse_dcf_runs` rows still carry pre-backfill `market_price_date`; they
  refresh on the next pipeline run.

## Two findings left for Lawrence
- `compactAmount(950_000)` → `"0.9M"`: gate and display disagree. Fix (a) changes
  1 input, (b) changes 461. **(b) recommended.** Written up in
  `frontend/app/components/ui/format.test.ts` — which exists only ON PR #71 until
  it merges, not on `main` or this branch.
- Byte-identical provenance order would need a sequence column on `score_inputs`.

## Next Action
Merge #69, then #73. Then write the first decision packet using
`_bmad-output/decision-packets/TEMPLATE.md` — one company, one real question.

## References
- `_bmad-output/planning-artifacts/foundational-decisions.md` — D9, D10, D3.2
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — `epic_catalog`
- `.claude/context/project-context.md` — Epic 6 learnings, 2026-08-13

---
*Generated by /session-end — 2026-08-13 19:02*
