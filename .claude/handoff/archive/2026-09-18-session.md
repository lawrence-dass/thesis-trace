# Handover — 2026-09-18 | Claude Opus 5

## Mode
Two PRs finished, green, and waiting on Lawrence. Working tree clean; nothing running.

## Focus
- **Task**: Story 13.3 close-out (PR #138) + a workflow diagnosis that became PR #141
- **Branch**: `claude/story-13-3-brand-mapping-2026-09-11` (main checkout), plus worktree
  `../ThesisTrace-workflow` on `claude/workflow-fixes-2026-09-18`
- **State**: complete-pending-review on both; no blockers of mine
- **Progress**: Epic 13 at 3/8. 13.3 answered the ZTS brand question, survived two Codex rounds.

## Resume Point
Nothing is half-done. The next session either (a) applies Lawrence's redline to the Epic 13
re-cut, or (b) runs `/post-merge` once #141 and #138 land. Both PRs are green and mergeable.

## Uncommitted Files
None — working tree clean.

## What Happened This Session
- **Story 13.3, the ZTS decision**: Lawrence chose "the concept means brands". Shipped as
  `us-gaap_v15`/`concepts_v16`, with a new `excluded_members` block (a known member that
  never resolves and never warns) because deleting the four ZTS members would have turned a
  decision into a nightly warning.
- **Codex round 2** found the fix half-done: CPB's own residual bucket was still landing in
  the brands concept, and `unmapped_member` suppression was keyed on the member NAME rather
  than the decision. Both verified against the live store, both fixed in
  `us-gaap_v16`/`concepts_v17` (`2e6de15`). A first attempt at the second fix was WRONG
  (treated `maps_to` as declining every other concept, which would have kept an impairment on
  the residual silent) and was reverted — see the commit body.
- **Throughput diagnosis** (Lawrence's ask): measured this project against `reslint`.
  13.3 took 19 commits over 8 days; reslint averages 1.15 commits/story and 4-8 stories/day,
  with a story file for every one of its 92 stories. ThesisTrace has 2. The `bmad-create-story`
  step lapsed after Epic 5, so stories had no exit condition.
- **Approval audit**: 47% of this project's 3,254 recorded Bash calls are unmatchable by ANY
  permission rule — 39% chain 3+ segments, 13% are heredocs, 8% source `.env`. The most-blocked
  segments in project history are `set +a`, `set -a`, `source ../.env`. Cause: the
  DATABASE_URL/TEST_DATABASE_URL separation was expressed as shell plumbing on every command.
- **PR #141** (worktree): CLAUDE.md story-workflow rules + a `Makefile` that absorbs the env
  plumbing + 50 pattern permissions. `make test` REFUSES when the two DB URLs match.
- **Epic 13 re-cut drafted** (13.4-13.8 -> 13 one-outcome stories). Not written to `epics.md`;
  awaiting Lawrence's redline and his numbering call.

## Decisions Made
- `brand_intangible_carrying_value` means BRANDS. ZTS keeps only `brands`; CPB's residual gets
  its own `brand_intangible_carrying_value_residual`; the aggregate keeps `_total`.
- Only a wholesale `excluded_members` entry suppresses `unmapped_member`. `maps_to` says where
  a member's carrying value goes, NOT that it can never be impaired.
- Workflow, decided by Lawrence 2026-09-18: reinstate story files (enforced by a test); split
  stories to one outcome; ship the fix and defer the finding; ask blocking decisions
  immediately with a recommendation. Approval pain is MECHANICAL — decisions and review
  handoffs are fine.
- Deferred on purpose: `excluded_members` is not axis-scoped and unreachable exclusions are not
  rejected (QSR's three franchise exclusions are inert). Recorded as
  `story_13_3_member_exclusions_are_not_axis_scoped`; belongs in the re-cut.

## Open Questions For Lawrence (all three are his, none block a session start)
1. Drop `Bash(TEST_DATABASE_URL=* *)` and `Bash(env -u * *)` from #141? Recommended yes, now
   that `make test` carries that traffic.
2. Epic 13 numbering: lettered (13.4a + a one-line regex change in `test_sprint_status.py`,
   recommended) or renumbered 13.4-13.16.
3. Merge order: #141 first, so 13.3 merges under the new rules.

## Context Needed
- Dev DB is at `concepts_v17` for all nine filers (rebuilt from stored raw facts, no EDGAR
  fetch). Older versions remain addressable. Re-canonicalize helper lives in the session
  scratchpad, not the repo — `make py F=...` is the intended home once #141 merges.
- Other sessions own PR #139 (`.env` fallback in the health test, worktree `ThesisTrace-envfile`)
  and PR #140 (golden harness cannot reach dimensioned facts). Not ours; don't touch.
- `next dev` rewrites `frontend/next-env.d.ts` — restore, never commit.
- `pkill -f "npm run dev"` does NOT kill the child `next-server`; kill the PID holding the port.
- Port 8000 is riskpulse. Render on 8001/3001 (`make api` / `make web` after #141).
- Commits NEVER carry Co-Authored-By/Claude-Session trailers.
- Local refs `backup/pre-trailer-strip-2026-09-17` and `backup/pass1-2026-09-17` can be deleted
  once #138 merges.

## Next Action
Wait on Lawrence. Then: merge #141, merge #138, `/post-merge` (13.3 -> `done` at
sprint-status.yaml:217), and apply the Epic 13 re-cut before 13.4 starts.

## References
- PRs: #138 (story 13.3), #141 (workflow + Makefile)
- `engineering-findings.yaml#story_13_3_zts_non_brand_intangibles_land_as_brand_value` (done)
- `engineering-findings.yaml#story_13_3_member_exclusions_are_not_axis_scoped` (open, deferred)
- `CLAUDE.md` — "Story workflow" and "Running things" sections land with #141
- project-context.md: `.claude/context/project-context.md` (2026-09-18 learnings)

---
*Generated by /session-end — 2026-09-18*
