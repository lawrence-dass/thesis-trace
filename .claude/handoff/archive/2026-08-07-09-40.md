# Handover — 2026-08-07 09:40 | Claude Opus 5

## Mode
Mid-task — Story 6.2 is half done. Handing to a Claude cloud session.

## Focus
- **Task**: Story 6.2 — free-cash-flow canonical concepts (`concepts_v8`)
- **Branch**: main (PRs #49–#55 merged; #13 closed; no branches, no open PRs)
- **State**: mid-task — live EDGAR verification DONE, spec files NOT written
- **Progress**: Epic 6 is 1 of 7 done, 6.2 in-progress. 245 passed / 1 skipped.

## Resume Point
**Story 6.2, second half. NO EDGAR ACCESS NEEDED — all seven CIKs were verified
live on 2026-08-07 and the results are recorded in `sprint-status.yaml` →
`story_6_2_live_verification`. Read that FIRST; it has per-filer, per-year coverage
and the exact source tags.** What remains:
1. `us-gaap_v7.yaml` — add `capex` (primary + OTEX's `PaymentsToAcquireProductiveAssets`
   variant) and `cash_and_equivalents` (plain tag, then the restricted-inclusive tag
   as priority 1).
2. `ifrs-full_v4.yaml` — add both concepts.
3. `derivations_v4.yaml` — add `cash_and_equivalents = Cash + CashEquivalents`,
   `kind: identity`, for BCE, which tags no combined concept.
4. `registry.yaml` → `concepts_v8`. Do not edit any spec an existing version points at (AD-2).
5. The four tests listed under `tests_required` in that same record.

## Uncommitted Files
None — working tree clean, nothing stranded locally.

## What Happened This Session
- Epic 6 decomposed into 7 stories (see the 07:02 archive copy for that context).
- Story 6.1 spike recorded; Story 6.2's live EDGAR verification done and recorded.
- Two prior bugs fixed and merged earlier (#49): OTEX's phantom maturity schedule,
  and no FX rate ever fetched for an IFRS filer.

## Decisions Made
- **BCE's cash is a DERIVATION, not a fallback.** It tags `Cash` and `CashEquivalents`
  separately (11y each), never the combined concept. An identity, not a decision.
- **QSR's restricted-inclusive cash tag is admissible, and provably so.** QSR files
  both tags FY2016–2018 and they are identical to the dollar, so it carries no
  restricted cash. The proof is FILER-SPECIFIC — it must be guarded by a test that
  fails when a filer with real restricted cash is added.
- **The interest caveat moves to Story 6.3.** 6.2's AC says annotate runs
  `computed_with_caveat`, but there are no runs yet — the solver is 6.3.
- **Cameco classifies interest as FINANCING, BCE as operating** (proven from IFRS
  classification tags, not inferred). So Cameco's `cash_from_operations` excludes
  interest paid and its free cash flow reads systematically high.
- **Suncor has no capex tag** — confirmed live. `insufficient_data` for all of Epic 6.

## Context Needed
- pytest needs `TEST_DATABASE_URL` set, `DATABASE_URL`/`LLM_API_KEY` unset.
- **Bucket coverage by `period_end` against the issuer's own FYE, never by the
  payload's `fy` field** — `fy` is the FILING's year, and using it produced a phantom
  gap in the 6.1 spike. OTEX's FYE is June 30, not December.
- `concepts_v7` is applied to local dev ONLY; a deployed environment still needs a
  canonicalization pass or both debt cards silently return empty.
- Golden entries must be extended in the SAME change as any capability (SM-1).
- For the GenAI/scope brainstorm rather than 6.2: `docs/PHASE-2-BRAINSTORM-BRIEF.md`
  §7 holds five open decisions with a recommendation on each.

## Next Action
Read `sprint-status.yaml` → `story_6_2_live_verification`, then write the four spec
files and the tests on a fresh branch off `main`. No EDGAR fetch required.

## References
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (`story_6_2_live_verification`, `story_6_1_reverse_dcf_coverage_spike`)
- `_bmad-output/planning-artifacts/epics.md` (Story 6.2 ACs; Epic 6 assumptions A-1..A-5)
- `backend/canonicalization/mappings/specs/` (us-gaap_v6, ifrs-full_v3, derivations_v3 — copy forward)
- `docs/PHASE-2-BRAINSTORM-BRIEF.md` (GenAI track + Track P, for the brainstorm)
- project-context.md: `.claude/context/project-context.md`

---
*Generated by /session-end — 2026-08-07 09:40*
