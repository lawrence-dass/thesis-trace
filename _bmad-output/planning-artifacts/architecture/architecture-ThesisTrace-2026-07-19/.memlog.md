---
scope: ThesisTrace whole-system architecture spine, derived from prd.md FR-1-21 and foundational-decisions.md D1-D7
purpose: build-substrate
altitude: initiative
updated: 2026-07-21T03:59
---

- (decision) Working mode: Coaching path. Purpose: spine only, build-substrate for solo builder Lawrence, no additional walkthrough artifact
- (decision) AD-candidate: Paradigm = Batch Pipeline (write path) + Read-Only Query API (read path), CQRS-style split. Pipeline internally structured as pipes-and-filters (EDGAR -> ingest -> raw store -> canonicalize -> validate -> score -> Postgres). Read path (FastAPI/Next.js) never computes or calls EDGAR live. User's own proposal, validated as correct fit over hexagonal/ports-adapters given batch-computation-dominant problem shape.
- (constraint) AD-candidate: Immutable raw storage + versioned canonicalization. raw_facts append-only keyed by (accession_number, content_hash); canonical_facts derived via versioned concept_mappings, never mutated in place.
- (constraint) AD-candidate: Deterministic Canonical Fact selection rules (ordered: prefer as-originally-filed over restated comparative, prefer least-dimensioned/most-specific member, prefer higher decimals precision). Unresolved ambiguity writes data_quality_issues with status needs_review - never arbitrary default.
- (constraint) AD-candidate: Dual-source ingestion - Company Facts API primary, raw Inline XBRL parse as audit/fallback for custom-taxonomy facts Company Facts omits.
- (constraint) AD-candidate: Versioned exact formula specification (equation, inputs, thresholds, rounding, missing-data policy, divide-by-zero policy) in a versioned config/spec artifact per formula, not hardcoded inline. Every score_run records the formula version used. Resolves ambiguity between named variants (Altman Z vs Z' vs Z", Beneish 8-var vs 5-var, Sloan balance-sheet vs cash-flow approach).
- (decision) AD-candidate: Append-only versioned score_runs; amendment (10-K/A) triggers a new score_run referencing new canonical_facts, prior run marked superseded not deleted/mutated. Resolves PRD Open Question 2 (restatement policy).
- (decision) AD-candidate: Deterministic-first explanation, LLM as constrained rewrite only - explanation generated as a template directly from score_results/canonical_facts with no LLM in the loop; LLM (when used) may only rewrite/polish already-correct text, never introduce new claims/numbers/citations. Meaningfully tightens PRD FR-12 beyond 'cited narrative' - flag as PRD-touching refinement to confirm with PM.
- (constraint) AD-candidate: Strict Python/TypeScript domain boundary - Python owns ingestion, canonicalization, all score computation; Next.js owns presentation only, no scoring/canonicalization logic, renders only what read API returns.
- (constraint) AD-candidate: EDGAR access discipline - identifying User-Agent, <=10 req/s (SEC fair-access policy), caching + retry/backoff, idempotent by (accession_number, content_hash), replayable ingestion jobs.
- (constraint) AD-candidate: Read path never triggers writes - all public FastAPI endpoints read-only against materialized Postgres; recompute/admin ops require a protected/operator-only path, never exposed anonymously. Ties to PRD D4 no-auth-for-users stance (auth, if any, is for Lawrence-as-operator only, not end users).
- (decision) Lawrence values quality/completeness over reduced scope - willing to invest more engineering effort rather than cut features when architecture surfaces a real gap. Applies across all three forks below.
- (decision) AD-candidate: Altman market-data gap resolved by adding ONE market-data source (Tiingo free tier, verified live) for period-end closing price only. Shares outstanding at FYE already free from EDGAR dei:EntityCommonStockSharesOutstanding (verified live against CP). EntityPublicFloat dei fact checked and rejected as substitute - measured at Q2 end, not FYE, wrong date. Altman stays in Phase 1 scope, computed correctly with real market value of equity, never book-value substitution.
- (version) Tiingo free tier: ~50 symbols/hour, 30+ years adjusted-close EOD data, official documented keyed API, no cost. Verified 2026-07-19. Backup/cross-check: FMP free tier (250 calls/day, EOD back to 1985).
- (decision) AD-candidate: Verdict kept as a real feature (not dropped), synthesis rule defined as transparent juxtaposition of each model's own published, cited threshold classification - Piotroski (8-9 Strong/5-7 Moderate/0-4 Weak), Altman (>2.99 Safe/1.81-2.99 Grey/<1.81 Distress, Altman's own published bands), Beneish (>-1.78 threshold, Beneish's own published cutoff), Sloan (needs explicit threshold chosen and cited per AD-5's versioned formula spec - least standardized of the four). Never a further blended/weighted single number across models - the stated rule IS 'show each model's own classification, uncombined.' Resolves PRD FR-9's undefined synthesis rule without compromising the four-honest-lenses positioning.
- (decision) MVP scope: confirmed build-order-only interpretation. Full PRD Phase 1 scope (4 companies, all 4 models including Altman via Tiingo, Comparison) remains the committed target. Shopify-only/Piotroski+Sloan-only list is internal walking-skeleton build sequencing, not a scope reduction.
- (question) PRD update needed after this architecture run: FR-4/FR-5 (Altman) needs a market-data-source note (Tiingo); FR-9 (Verdict) needs the per-model-threshold-juxtaposition synthesis rule stated explicitly, replacing the undefined 'stated-rule synthesis' language; FR-12 (AI Score Explanation) needs the deterministic-template-first, LLM-as-constrained-rewrite-only tightening.
- (version) Stack pinned (verified 2026-07-19): Next.js 16.2.10, React 19.2.7, FastAPI 0.136.x, Pydantic 2.10.x, SQLAlchemy 2.0.36 (async AsyncSession, standard FastAPI DB-access pattern), Python 3.12/3.13. Supabase provisions Postgres 17 on new projects (PG14 deprecated 2026-07-01).
- (constraint) Supabase free tier: 500MB DB, 5GB egress, projects auto-pause after 7 days DB inactivity (manual unpause required). Mitigation: the daily/scheduled batch ingestion job itself doubles as the keep-alive ping - no extra cost or complexity needed.
- (decision) AD-candidate: Backend hosting = Render (Web Service ~$7/mo always-on + Cron Job service ~$1-3/mo, both first-class on one platform/bill) for both the FastAPI read API and the scheduled batch pipeline. Chosen over Fly.io (cheaper but no built-in cron, more DevOps overhead) and Railway (no free tier, per-service billing) per developer-productivity-is-architecture principle for a solo builder.
- (decision) Frontend hosting: Vercel free/hobby tier for Next.js - confirmed viable at hobby traffic scale, no concerns found.
- (version) Cost check against $25/month ceiling: Render ~$8-10 + Supabase free ($0) + Tiingo free ($0) + Vercel free ($0) = ~$8-10/mo fixed, leaving ~$15-17/mo headroom for LLM API costs. Comfortable fit.
- (decision) AD-candidate: new market_prices entity added (not in original brainstorm) - issuer_id, price_date, close_price, source (Tiingo/FMP), fetched_at. Required by the Altman resolution (Tiingo closing price, joined with EDGAR shares-outstanding, to compute market value of equity).
- (decision) Formula specs (AD-5's versioned equations/thresholds/rounding policies) live as versioned config/spec files in the codebase (e.g. formulas/piotroski_v1.yaml), not a DB table - score_runs.formula_version references the spec version by string. Keeps formula logic code-reviewable and diffable rather than buried in DB rows.
- (constraint) AD-candidate: All financial figures stored/computed as NUMERIC/DECIMAL in Postgres, never FLOAT/DOUBLE. Prevents silent floating-point rounding divergence between independently-built calculation code, which would break golden-dataset matching (SM-1) in ways that are hard to trace.
- (constraint) Consistency conventions: internal PKs are UUID (except issuers keyed by CIK string, filings keyed by SEC accession_number string - natural external keys). Dates: DATE for fiscal/filing dates, TIMESTAMPTZ for computed_at/fetched_at, ISO 8601 throughout. API error envelope: single documented JSON shape {error: {code, message, details}} across all FastAPI endpoints. Config: env vars only for secrets (EDGAR contact, Tiingo key, LLM key, DB connection) - never hardcoded. Auth: no end-user auth in Phase 1 (PRD D4); admin/recompute endpoints protected by a shared-secret header, operator-only, not a full auth system.
- (decision) Environments: single production environment + local dev (Python/Node run locally against a local or dev Supabase project), no formal staging tier - matches hobby/solo scale and the PRD's explicit NFR deferral to Phase 4.
- (direction) Deferred: Phase 2 entities (thesis/thesis_diff tables), Phase 3 entities (deep_research_requests, notifications), LangGraph Filing Q&A orchestration internals, full auth system (startup-optional pivot per D4), SEDAR+/TSX-only ingestion pipeline (per PRD Non-Goals), exact accounting-identity validation rule set (data_quality_issues tracking exists now, specific checks are implementation detail), Sloan accruals exact threshold value (pinned in versioned formula spec at implementation time, not spine-level), CI/CD/monitoring/rollback (Phase 4 per PRD).
- (event) Finalize step 2 (input reconciliation): ran vs prd.md - found AD-11 (Tiingo) silently conflicted with locked foundational-decisions.md D7 'no multiple data providers' constraint, unlike AD-7 which was correctly flagged as PRD-touching. User confirmed: amend D7 explicitly as a narrow, scoped exception (Tiingo fills a gap EDGAR can't, not redundant multi-provider complexity). foundational-decisions.md D7 updated 2026-07-19 to record this exception. Also fixed minor gap: Render cost annotation added to spine Stack table (~$8-10/mo). No PRD Non-Goals violated; OQ2 confirmed resolved by AD-6.
- (event) Run paused by user 2026-07-19 mid-Finalize, to switch to mobile/cloud-session workflow. Status at pause: Step 1 Distill = done (ARCHITECTURE-SPINE.md written, 15 ADs). Step 2 Reconcile = done (reconciled vs prd.md, found + resolved AD-11/D7 conflict, fixed Render cost annotation). Step 3 Reviewer Gate = STARTED BUT INCOMPLETE: lint_spine.py ran clean (0 findings). Rubric walker and the 2 configured finalize_reviewers (web-research verification lens, adversarial two-independent-units lens) were dispatched but user interrupted before they completed - reviews/ subfolder created but empty, no review files written. RESUME POINT: re-dispatch the rubric walker + 2 finalize_reviewers against the current ARCHITECTURE-SPINE.md, then continue Steps 4 (triage) - 7 (close, set status:final).
- (event) Finalize resumed (cloud session 2026-07-21). Reviewer Gate ran: lint_spine.py clean (0). Three lenses dispatched as parallel subagents vs ARCHITECTURE-SPINE.md — rubric-walker (pass-with-fixes), version-verification (pass w/ minor), adversarial-divergence (conditional). Full reviews in reviews/. Parent web-verified disputed facts.
- (version) Version re-verification (2026-07-21, live npm/PyPI): Next.js 16.2.10 + React 19.2.7 CONFIRMED current (rubric-walker's '16.2.10 does not exist' was wrong). Python pins stale — refresh Stack: FastAPI 0.136.x->0.139.2, Pydantic 2.10.x->2.13.4, SQLAlchemy 2.0.36->2.0.51. Postgres 17, EDGAR, Tiingo, Render, Supabase all confirmed current.
- (decision) AD-16 (from adversarial F1, critical): tri-state signal status. Every per-signal/per-component result carries an explicit status enum {pass, fail, insufficient_data}; a missing input is insufficient_data, never NULL and never coerced to a failing 0 downstream. Enforces FR-3 no-guess/no-default across scoring->api->frontend.
- (decision) AD-17 (from adversarial F2, critical): single data_quality_issues contract + owner. One documented row shape and a closed status enum {needs_review, resolved, dismissed}; canonicalization and validation both APPEND issues but only an operator path mutates status. No unilateral in-place status rewrite by either pipeline stage.
- (decision) AD-18 (from adversarial F3): canonical score_results contract. Fixed row shape (one row per model per signal/component: score_run_id, model, signal_key, value NUMERIC, status [AD-16], threshold ref) with a stable snake_case signal_key vocabulary pinned in each formula spec (AD-5). No EAV-vs-JSON-blob or key-naming divergence across scoring/explanation/api.
- (constraint) AD-5/AD-15 tightened (from adversarial F4): one shared rounding policy. Formula specs declare rounding via an enumerated mode (default ROUND_HALF_EVEN) applied by one shared decimal engine, not free-form per spec. Closes threshold-boundary classification divergence between independently-built evaluators that AD-15 (NUMERIC) alone did not.
- (constraint) AD-8/AD-12 tightened (from adversarial F5): threshold classification computed backend only. The band label is precomputed in scoring and stored in score_results; frontend renders the stored label and never hardcodes or recomputes band cutoffs. Removes Verdict-ownership ambiguity between AD-8 and AD-12.
- (decision) AD-19 (from rubric H2): Provenance contract pinned as invariant. Every displayed canonical value and every score input carries a provenance reference {accession_number, xbrl_concept/line item, filing period, source} resolvable end-to-end. Read API returns provenance with the value; frontend links to it. Core traceability promise is a spine invariant, not convention.
- (decision) AD-20 (from rubric H1): sector-scope applicability representation. Each model result carries an applicability state {computed, excluded_out_of_scope, computed_with_caveat} per D6 (financials excluded from Altman/Beneish; capital-intensive firms carry an interpretive caveat). Scoring sets it; api/frontend render it uniformly — never a bare number for an excluded/caveated case.
- (decision) AD-21: FR-12 LLM pinned (user-confirmed 2026-07-21). Small cheap model, default Anthropic Claude Haiku 4.5, key via env var, provider swappable. Constrained-rewrite only per AD-7 — never in the numeric loop. Fits the ~25 USD/mo ceiling.
- (decision) Piotroski band correction (user-confirmed 2026-07-21, from version-review finding): AD-12 to use Piotroski's own published classification — Strong=8-9, Weak=0-1, 2-7 shown as Middle/mixed (no invented cutoff). Prior 8-9/5-7/0-4 tripartite was non-canonical, violating AD-12's own 'published, cited threshold' rule. Exact display copy + citation pinned in the formula spec (AD-5).
- (decision) Golden-dataset harness (from rubric M2): Phase-1 golden-dataset regression harness for SM-1 (all four models vs hand-verified/published values) is in Phase-1 scope; CI automation of it stays Phase-4-deferred. Recorded so the SM-1 test dimension is not silent.
- (constraint) Seam pins (from adversarial F6-F9): dual-source tiebreaker (Company Facts primary wins on conflict, Inline XBRL only fills omissions, divergence logs a data_quality_issue); FYE market price = close on the last trading day <= FYE (AD-14), never a live fetch; the 'current' score_run per issuer/model is the latest non-superseded run (AD-6); a lens not yet covered for a company returns an explicit success-envelope 'not_available' state, never an error and never a fabricated zero.
- (event) spine finalized — status flipped draft->final, updated 2026-07-21. Reviewer Gate complete: applied version-pin refresh, mermaid Vercel fix, AD-16..AD-21 (tri-state status, single DQ-issue contract, score_results contract, provenance invariant, sector applicability, FR-12 LLM), tightened AD-4/5/6/8/12/14/15, Piotroski bands corrected to paper, golden-dataset harness noted Phase-1, not_available envelope convention. 21 ADs total.
